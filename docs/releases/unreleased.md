# 下一版本（尚未分配版本号）

- 发布状态：已合并到 `main`，尚未发布
- 记录日期：2026-09-13
- 上一稳定版本：`v3.8.0`
- 稳定版本提交：`f52ba8691250d4b364a21d2474bb760b74eec106`
- `main` 合并提交：`245a4691588ae32d74ed483ae9bff4b023f6b025`
- 当前 `VERSION`：`3.8.0`

## 摘要

本次更新集中重构设置流程、播放栏、首页交互、Dock 专辑封面和歌曲列表，并加入播放状态恢复。内容已经合并到 `main`，但在正式确定版本号、更新 `VERSION`、构建发布包并创建标签之前，仍视为未发布。

## 用户可见更新

### 新增

- 设置中新增“记住上次播放状态”，重启后恢复歌曲、队列、进度和音量
- 设置界面改为主窗口内嵌页面，不再打开独立设置窗口
- 增加深色、浅色两套 App 图标资源
- 设置中增加“自动 / 深色 / 浅色”三档 App 图标选择
- 用户歌单管理流程和设置页导入入口
- 首页“最近添加”区域
- 首页问候语增加中文诗句、简短问候和英文短句轮换

### 改进

- 设置界面统一为主界面一致的白蓝玻璃卡片风格
- 应用菜单和快捷键设置入口切换为主窗口内设置页面
- 底部播放栏缩为 72pt，并缩小专辑封面和播放按钮
- 主界面移除“导入”和“添加音乐”按钮，导入统一放入设置
- 顶部“漫域音乐”品牌和图标支持点击返回首页
- 首页歌词摘要固定展示 4 行，并优化中段截取和制作人员过滤
- 首页播放按钮移入专辑封面内部，并根据封面主色自动配色
- 首页副标题只显示歌曲总数
- 放大播放主界面左侧封面与下方控制条，并优化歌词渐隐和当前行强调效果
- Dock 专辑封面增加窄幅玻璃背板，兼容不同尺寸封面并遮挡白边
- 调整 Dock 专辑封面留白，缩小封面并扩大亚克力背板的可视边框
- Dock 专辑封面角标改为封面取色的玻璃亚克力样式，并放大播放或暂停符号
- Dock 基础图标和应用侧栏图标跟随图标设置
- FLAC 等格式标签移动到歌曲信息右侧
- 启动焦点默认落在播放按钮，未编辑搜索框时按空格可播放或暂停

### 修复

- 切歌时保留上一张封面，消除 Dock 图标闪回默认图标
- 修复歌曲列表顶部大块空白，列表首行紧跟表头
- 首页“最近播放”在当前停留期间保持顺序，不再随切歌跳动
- 首页歌词竖线与文本严格等高，避免底部空白
- 修复侧栏品牌图标与 App 图标不一致
- 修复 README 被源码内容错误覆盖的问题，并重建项目说明

## 技术变更

- `AudioPlayer` 增加播放状态持久化与恢复
- `README` 改为稳定的项目入口文档
- 新增按版本独立保存的发布记录、历史版本归档和发布模板
- `AGENTS.md` 与 `docs/VERSION_CONTROL.md` 增加发布文档强制规则
- 歌曲列表由旧 `List` 布局改为受控高度的 `ScrollView`、`LazyVStack`
- 设置模型和应用图标管理逻辑拆分

## 兼容性与迁移

- 最低系统版本仍为 macOS 14
- 原有资料库、歌单、收藏和播放历史继续使用现有存储格式
- 首次启用“记住上次播放状态”后，会在退出或状态变化时写入播放状态
- 本次合并没有删除历史 `CHANGELOG.md` 条目

## 验证

- 功能分支构建：`swift build -c release` 通过
- 应用包构建：`./scripts/build-app.sh` 通过
- 应用签名：`codesign --verify --deep --strict` 通过
- 手工验证：歌曲页顶部空白、列表滚动、设置页、播放栏和播放状态恢复
- 合并到 `main` 后重新执行 Release 构建和签名验证

## 发布前待办

- 确定正式版本号并更新 `VERSION`
- 将本文件复制为 `vX.Y.Z.md` 并补齐发布日期、版本提交和标签
- 严格执行 `docs/VERSION_CONTROL.md` 中的发布流程
- 创建注释标签并推送 `main` 与标签
- 使用 `git ls-remote` 核验远端分支和标签
