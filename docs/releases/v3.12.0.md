# 漫域音乐 v3.12.0

- 发布状态：已发布
- 发布日期：2026-09-18
- 版本提交：`42e411f`、`e0ae8f3`、`dcf5199` 及发布文档提交
- 合并提交：合并后于正文补充
- Git 标签：`v3.12.0`
- 对应 `VERSION`：`3.12.0`
- 上一稳定版本：`v3.11.0`
- 发布分支：`codex/dock-playback-badge`
- 发布产物：`漫域音乐-3.12.0.dmg`（UDZO 压缩镜像，内含 `漫域音乐.app` 与 `/Applications` 拖拽链接）

## 摘要

v3.12.0 是首个提供 DMG 安装镜像的公开发布版本：播放控制融合为「三态播放模式钮 + 列表⇄气泡整页队列选歌」，切歌时歌词不再闪现等待占位（新增会话级歌词缓存与全库预热）；Dock 封面右下角播放徽标放大为蓝圆白边样式；多选入口移到列头并支持批量加入歌单；修复了「在设置中移除音乐文件夹后歌曲数量不减少」的数据一致性问题，并把音乐来源、屏蔽文件夹清单持久化进资料库。

## 用户可见更新

### 新增

- 首个公开 DMG 安装镜像：下载 `漫域音乐-3.12.0.dmg` 后双击挂载，把「漫域音乐」拖入「应用程序」即可使用。
- 新增播放页内部「列表 ⇄ 气泡」融合切换钮（位于原循环按钮位置，即上一首按钮左边）：在播放页点列表图标，封面+歌词区域整块切换为当前播放队列的选歌列表（不离开播放页），点任意歌曲立即切换播放，再点气泡图标切回封面+歌词，图标随状态 morph。队列列表行可右键「从队列移除」；队列为空时显示空状态提示。
- 设置 → 资料库新增「音乐来源」清单：面板导入与拖入添加的文件夹/文件都会记录并显示名称、完整路径与类型图标，可逐项点「−」移除（连带删除其下歌曲）。
- 新增「不扫描短音频」开关：开启后导入自动过滤时长不足 60 秒的音频。
- 新增「屏蔽文件夹」清单：导入扫描时跳过指定子目录，可随时取消屏蔽。

### 改进

- Dock 封面右下角播放状态徽标放大重做：保留原有的专辑封面底板，右下角徽标直径从 94pt 放大到 140pt（贴右下角，右缘留 16pt、底缘留 6pt）；颜色不再从封面调色板提取，改为固定纯蓝圆底（顶部 sRGB(0.22,0.52,0.97) → 底部 (0.14,0.43,0.92) 的近实色竖向渐变）+ 10pt 白色圆描边 + 中央白色播放状态符号（播放中为两根全圆角暂停竖条：宽 13.5%、高 40%、间距 12%；暂停时为等比放大的白色播放三角），带柔和投影。
- 歌曲列表多选入口位置调整：原顶部工具栏的「✓ 编辑」胶囊按钮移到歌曲列表列头「时长」右侧，图标中心与下方歌曲行的爱心按钮居中对齐，仅显示勾选圆图标（去掉「编辑」文字）；点击图标后，全选、删除所选等纯图标按钮像子菜单一样从图标右侧滑出（浮层，不挤占列宽），图标自身变为实心高亮，再点一次即完成退出，顶部搜索栏右侧不再有多选胶囊与批量操作条。多选时新增批量「添加到歌单」：列头操作区的文件夹＋图标弹出歌单菜单（含「新建歌单…」，新建后自动把所选歌曲装入新歌单），歌曲行右键/「⋯」菜单在多选时也变为「添加到歌单（已选 N 首）」，一次性加入全部选中歌曲、歌单内自动去重。
- 随机播放、顺序播放、单曲循环三种模式融合为一个播放模式按钮：每点一下切换一个模式（顺序播放 → 单曲循环 → 随机播放 → 顺序播放），图标随模式 morph：顺序播放为灰色顺序箭头、单曲循环/随机播放时图标高亮。播放页与底部播放条两处同步。
- 菜单栏「播放」菜单：原「循环模式」子菜单 + 「随机播放」开关合并为「播放模式」子菜单（顺序播放 / 单曲循环 / 随机播放，当前模式打勾）。
- 首页快捷操作「随机播放」现在会正确同步三态播放模式状态。
- 歌词居中补偿：刚播放到前几句时当前行被居中会导致上方空一大块。`centerActive` 增加「leadCompensation」补偿——activeIndex=0 往上推 36pt，activeIndex=4 时归零，线性衰减，叠在原逐行动画上过渡自然。
- 歌词预加载：资料库加载完成后后台逐项低优先级解析全部曲目的内嵌歌词与同名 LRC；播放期间每加载一首歌曲，也会提前发起队列接下来两曲的歌词读取。切换歌曲时歌词直接显示，不再先闪一段“暂无歌词/正在等待歌词”再跳成正文。
- 歌词占位状态区分加载中与确认无歌词：正在读取时显示“正在载入歌词”；确认歌曲确实没有内嵌歌词或 LRC 文件时才显示“本歌曲暂无歌词”。
- 连续快速切歌时歌词面板做极轻“呼吸”：切歌瞬间整块歌词内容透明度下沉到 0.5，约 0.14 秒内恢复，盖住列表重排顿挫；歌词视图不做销毁重建，零布局开销。
- 连续快速切歌时立即暂停播放：相邻两次切歌间隔小于 0.7 秒即判定为快切，快切期间保持暂停，切歌停顿 0.4 秒后只播放最后选定的最新一首。慢速点歌（间隔大于 0.7 秒）仍立即播放，行为不变。

### 修复

- 修复设置中移除音乐文件夹（来源）后歌曲数量不减少的问题：来源清单此前只存在于内存，重新添加文件夹会触发扫描，而扫描进行中移除来源时在途导入落库会把刚删掉的歌曲全部合并回来。现在来源会持久化显示，移除来源立即删除其下歌曲，并同步清理收藏/播放历史/歌单引用；扫描进行中移除也有来源前缀级屏蔽，歌曲不会复活；路径统一标准化并解析符号链接，软链/别名形式选中的文件夹同样生效。
- 修复每切换一首歌曲歌词区域都先显示等待提示、几百毫秒后再替换为歌词造成跳动的问题（大部分歌曲本来就有歌词）。
- 修复快速连切时播放条虚走、恢复播放后跳回 0 的问题：切歌耗时不再计入播放进度，快切期间进度条冻结在真实媒体时间。

## 技术变更

- `LibraryStore` 修复移除来源不删歌：
  - `PersistedLibrary` 新增 `sources: [LibrarySource]` 与 `blockedFolderPaths: [String]`，均为 `decodeIfPresent ?? []`，旧库文件无损兼容；`scheduleSave` / `readLibrary` / `apply(.loaded)` 全链路往返。
  - `LibrarySource` 增加 `Codable, Sendable`。
  - 新增 `normalizedPath(_:)`（`standardizedFileURL.resolvingSymlinksInPath().path`），导入去重、在途复活检查、单曲删除、来源移除四处比较全部改用归一化路径；新增 `resolvesAsDirectory(_:)`（`FileManager.fileExists(isDirectory:)`，跟随软链）修正软链目录被误判成单曲来源。
  - `removeLibrarySource(_:)` 重写：校验 `canEdit`、按归一化前缀/精确匹配收集 `removedIDs`，一次性清理 `tracks / favoriteIDs / history / playlists.trackIDs`；导入进行中除登记单曲路径外，再向 `removedSourcePathsDuringImport` 登记来源前缀，在途枚举结果在三个落库检查点（newURLs 过滤、accepted 过滤、generation 冲突合并）统一经 `isImportBlocked(_:)` 拦截。
  - `addBlockedFolder` / `removeBlockedFolder` 变更后调用 `libraryContentDidChange()` 持久化；删除遗留调试 `print`。
- 新增 `Tests/HarmonyPlayerTests/LibrarySourceRemovalTests.swift`（7 个用例：文件夹来源移除删歌、多来源互不株连、来源重启持久化、旧库曲目在重新添加来源后再移除、软链路径来源移除、导入进行中移除不复活并落盘、移除时清理收藏/历史/歌单；测试内现场生成合法 PCM WAV，不读取用户音乐目录）。
- `DockArtworkController.makeDockIcon` 封面底板、调色板提取、后台串行渲染队列与代数号全部保留；仅替换右下角徽标绘制段：徽标直径 94 → 140pt，位置由 `(maxX-94-14, minY+14)` 改为 `(maxX-140-16, minY+6)`；删除封面色三色渐变、玻璃高光盘与左上白色弧线，改为固定蓝色 `NSGradient`（angle -90）+ 10pt 白色内缩圆描边；暂停双竖条按徽标直径比例（13.5%/40%/12%，全圆角），播放三角按 `badgeSize/94` 等比放大。
- 新增 `PlaybackMode`（`Models.swift`）：`sequential / singleRepeat / shuffle` 三态枚举，提供 `systemImage`、`helpText` 与 `next`；`AudioPlayer` 新增 `playbackMode` 与 `setPlaybackMode(_:)` / `cyclePlaybackMode()`，同步底层 `isShuffle` 与 `repeatMode`。
- `PlayerBar` 删除不再使用的 `QueuePickerMorphSymbol`；`QueuePanel.swift` 删除不再使用的 `QueuePickerView`；`TrackListView` 新增 `onRemoveTrack` / `removeTrackLabel` 入参。
- 多选状态从 `MainView` 下沉到 `TrackListView` 内部自管；列头新增全选钮与浮层式批量操作区（添加到歌单/删除）；时长列统一 48pt 左对齐并左移 6pt 与行时间严格对齐；`MainView` 删除顶部编辑胶囊与批量操作条。
- 新增 `LyricsCache`：NSLock 保护的会话缓存，同一首歌只允许一次磁盘 IO，解析为空按无歌词缓存；`preload(_:)` 全库低优先级预热，`preloadNext(_:)` 预热队列邻近两曲，内存压力时可清空。
- `EmbeddedMetadataReader` 新增 FLAC `readLyrics(from:)` 轻量路径：只读 VORBIS_COMMENT 块、seek 跳过 PICTURE 块。
- `AudioPlayer` 歌词加载统一改为 `prepareLyrics(for:)`（命中缓存则同轮落位），新增 `prefetchUpcomingLyrics()`；新增快切暂停机制（0.7s 窗口、0.4s 恢复、代数号保证最后一次生效），快切期间墙钟插值冻结。
- `LongSessionMemoryTests` 合成曲目由 0.3s 调整为 4s（快切修复后旧短曲会自动切歌，旧断言依赖被修复的滞后 bug）。
- 新增 `Tests/HarmonyPlayerTests/PlaybackModeTests.swift`（3 用例）与 `LyricsCacheTests.swift`（5 用例）。

## 兼容性与迁移

- 最低系统版本：macOS 14（arm64）。
- `library.json` 新增 `sources` 与 `blockedFolderPaths` 两个字段；读取端对旧文件 `decodeIfPresent` 回退空数组，旧版本库可直接打开，不产生迁移。由旧版本升级后来源清单首次为空，重新添加一次文件夹即可恢复显示（不会重复导入已有歌曲）。
- 应用为本地 ad-hoc 签名（无 Apple 开发者公证）：在他人 Mac 上首次打开若 Gatekeeper 提示「无法验证开发者」或「已损坏」，请在应用上点右键 → 「打开」确认一次，或在「系统设置 → 隐私与安全性」中允许；之后即可正常使用。
- 歌词缓存为会话内存缓存，格式不变。

## 验证

- 自动化测试：`swift test --disable-sandbox` 全套 42/42 通过（本次新增 `LibrarySourceRemovalTests` 7 用例）。
- Release 构建：`swift build -c release --disable-sandbox` + `zsh scripts/build-app.sh` 成功，`CFBundleShortVersionString=3.12.0`、`CFBundleVersion=162`。
- 应用包验证：`codesign --verify --deep --strict` 通过（valid on disk / satisfies its Designated Requirement，ad-hoc 签名）。
- DMG 验证：hdiutil 挂载只读镜像成功，卷内包含 `漫域音乐.app` 与 `Applications` 软链；卷内 app 版本号 3.12.0、签名校验通过；卸载正常。
- 产物：`dist/漫域音乐-3.12.0.dmg`（约 8.4 MB，UDZO），SHA-256：`0c5b00f5123ff60ae3883675e5534936ae7e634c84cf6acff6d597cfa3a6fdd3`。
- 手工验证：Release 副本实际运行，已验证应用启动与资料库读取；Dock 徽标样式、播放模式三合一、多选批量歌单由用户在验收构建中确认。

## 已知问题与后续

- 随机模式下一首不可预测，极早期（全库预热尚未跑到该曲目时）随机跳到未预热曲目仍可能短暂显示“正在载入歌词”；预热完成后不再出现。
- 未做 Apple Developer ID 签名与公证，他机首次打开需右键确认（见兼容性说明）。

## 变更来源

- 功能分支：`codex/dock-playback-badge`
- 相关提交：
  - `42e411f` feat: Dock 图标改为大号蓝白播放状态图标
  - `e0ae8f3` fix: 保留专辑封面底板，仅将右下角播放徽标放大改为蓝白样式
  - `dcf5199` fix: 移除音乐文件夹来源时正确删除歌曲，并持久化来源清单
- 文档与发布提交：合并后于正文补充

## 修订记录

暂无。
