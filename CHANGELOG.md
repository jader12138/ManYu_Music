# 更新日志

## Unreleased

- 播放页专辑图下方控件区最右侧新增收藏爱心：点击收藏/取消收藏当前歌曲，收藏后呈实心并高亮。
- 歌词滚动流畅化：修复自动换行时的顿挫感——歌词列表此前随播放时钟每秒整表重算十余次（现已改为仅当前行变化时刷新），并保留普通 VStack、恒定行排版；点击歌词跳转改为更慢的匀速多段滑行（起段加速、末段减速、中段速度连续），与进度条动画互不拖累；歌词整体位置略微上移。
- 播放条与播放页进度条改为自绘平滑进度条：用等长 linear 缓动把时钟更新接成恒速滑行，彻底消除"一跳一跳"；悬停/拖动显示圆点滑块（出现时直接位于当前播放位置），拖动时实时跟手。

- 窗口左上角的红黄绿三个系统按钮整体往右、往下微调，不再紧贴窗口左上角。
- 首页快捷操作顺序调整为："继续播放"在最前，其后依次是"播放全部""随机播放"，三个按钮使用同一套悬停动效。
- "继续播放"跨启动记忆说明：设置 → 播放记忆（默认开启）会保存关闭软件时的歌曲、队列与进度；重新打开后点"继续播放"即从上次位置接着播。

- 歌曲列表排序改为点击列头完成：点击"标题"按歌名字母序、"专辑"按专辑名字母序、"时长"按时长排列；再点同一列头切换升/降序。激活列头右侧显示深色小尖角（升序向上、降序向下），列头文字位置和颜色不受影响。
- 歌曲页搜索框左侧的原排序按钮已移除，其功能融合进"标题 / 专辑 / 时长"三个列头；未点击列头时保持原有的添加时间顺序。

- 界面风格向椒盐音乐式的扁平简洁靠拢：侧栏去掉渐变玻璃底改为纯色平面，选中项改为主题色浅色圆角块（移除左侧竖条）；底部播放条改为实色平面。首页"推荐曲目"卡片保留原有玻璃质感。
- 新增统一设计令牌：`HPMetrics` 圆角层级（行 10 / 卡片 16 / 瓦片 20）与 `Color.hpHairline` 发丝线，后续视图统一引用。
- 全应用交互动效升级（对齐 Apple Music 风格）：默认无背景的按钮悬停时以圆角矩形渐显主题色高亮，按下加深；主操作按钮悬停轻微提亮上浮。侧栏导航、设置页签、详情页工具按钮、显示切换器、歌词样式按钮均已接入。
- 所有交互按钮取消椭圆（胶囊）造型，统一改为圆角矩形；主操作按钮为实色圆角矩形。
- "最近添加"列表模式去除歌曲行下的常驻灰底，改为悬停时才显示高亮，与底色更融合。
- "最近添加"改为按歌曲展示：卡片显示歌曲名与演唱者（不再显示专辑名），封面调小、歌名与演唱者更紧凑。
- "最近添加"右上角新增显示方式切换：封面网格 / 横向文件名列表，选择会记住。
- 首页改版：移除"最近播放"横滑区块，"最近添加"改为纵向平铺网格（每行约 5~6 张卡片，随窗口宽度自适应，不再横向滚动）。
- 首页快捷操作新增"继续播放"按钮（恢复上次播放进度）。
- 切换白天/夜间模式时，左上角品牌图标同步切换：白天显示白色图标，夜间显示深色图标，并带淡入过渡（遵循"减弱动态效果"设置）。
- 左上角品牌图标不再受设置中"应用图标"风格固定的限制；该设置现在只影响 Dock 图标。
- 修复"每次打开软件时更新"的首页推荐在切换页面后被重新随机的问题：推荐现在在整个运行期间保持稳定，重启应用后才更换。
- 设置中的推荐频率选项更名为"每次打开软件时更新 / 每天更新"，并说明切换页面时推荐保持不变。
- 大曲库浏览更流畅：列表、专辑、歌手、文件夹的分组与搜索改为后台预计算，一次事务刷新界面。
- 封面按显示尺寸三级降采样加载，长列表滚动更快、内存更低；小尺寸缩略图不再叠加投影。
- 播放进度与睡眠定时器倒计时独立刷新，不再触发整个界面重算。
- 播放/暂停图标与详情页切换动画遵循系统"减弱动态效果"设置。
- 新增 20 个单元测试，覆盖资料库持久化、浏览快照、封面管线与播放时钟隔离。

## 3.10.0 - 2026-09-13

- 播放页窗口标题栏改为完全透明，顶部不再出现独立色带；返回按钮改为左上角圆角 V 形，并补充随机、循环控制。
- 播放页布局按窗口尺寸伸缩，减少底部留白；歌词样式面板增加行距和显示行数。
- 调整开发流程为一功能一分支，合并和发布完成后删除已用完的分支。
- 增加功能完成或成功合并后的固定结束语规则。

## 3.9.0 - 2026-09-13

- 新增按版本独立保存的发布记录、历史版本归档和发布模板。
- 修复 README 被源码内容覆盖的问题，恢复项目说明并补充文档索引。
- 放大播放主界面左侧封面与下方控制条，并优化歌词渐隐和当前行强调效果。
- 调整 Dock 专辑封面留白，缩小封面并扩大亚克力背板的可视边框。
- 设置中新增“记住上次播放状态”，重启后恢复歌曲、队列、进度和音量。
- 启动焦点默认落在播放按钮，未编辑搜索框时按空格可播放或暂停。
- 修复歌曲列表顶部大块空白，列表首行紧跟表头。
- 首页“最近播放”在当前停留期间保持顺序，不再随切歌跳动。
- Dock 专辑封面增加窄幅玻璃背板，兼容不同尺寸封面并遮挡白边。
- 顶部“漫域音乐”品牌和图标支持点击返回首页。
- 切歌时保留上一张封面，消除 Dock 图标闪回默认图标。
- 设置界面改为与主界面一致的白蓝玻璃卡片。
- 设置界面改为主窗口内嵌页面，不再打开独立设置窗口。
- 应用菜单和快捷键设置入口改为切换到主窗口的设置页面。
- FLAC 等格式标签移动到歌曲信息右侧。
- Dock 专辑封面角标改为封面取色的玻璃亚克力样式，并放大播放/暂停符号。
- FLAC 等格式标签移动到歌曲名和歌手右侧。
- 设置入口移动到左侧栏底部。
- 主界面移除“导入”和“添加音乐”按钮，音乐导入统一放入设置。
- 底部播放栏缩为 72pt，并缩小专辑封面和播放按钮。
- 首页歌词摘要固定展示 4 行。
- 首页播放按钮移入专辑封面内部，并根据封面主色自动配色。
- 首页副标题只显示歌曲总数，不再显示专辑数量。
- 首页歌词竖线与文本严格等高，避免底部空白。
- 首页专辑封面增加玻璃材质衬托层。
- 播放状态按钮改为无白色描边的玻璃亚克力样式。
- 首页歌词摘要改为优先截取歌词中段，并过滤制作人员信息。
- 首页专辑封面支持点击进入完整播放界面，右下角状态按钮支持播放/暂停。
- 首页问候语增加中文诗句、简短问候和英文短句轮换。
- 增加深色、浅色两套 App 图标资源。
- 设置中增加“自动 / 深色 / 浅色”三档 App 图标选择。
- Dock 基础图标和应用侧栏图标会跟随图标设置。
- Dock 专辑封面保留为播放设置中的独立开关，不放入图标选择。
- 修复侧栏品牌图标与 App 图标不一致。
- 增加强制分支开发、合并提醒和功能文档规则。

本项目使用语义化版本号：`主版本.次版本.修订版本`。

## 3.8.0 - 2026-09-13

- 修复 macOS Dock 图标白边和底部裁切问题
- 重新生成透明圆角、多尺寸 AppIcon
- 新增设置窗口，可从侧边栏齿轮或系统设置菜单进入
- 新增播放时在 Dock 显示专辑封面
- Dock 专辑封面增加白色播放/暂停状态角标
- 首页推荐歌词改为连续显示 3～4 行
- 歌曲列表和首页封面右下角统一使用自适应播放状态图标
- 修复歌词页控件底色突兀、蓝色光晕和标题栏冗余问题
- 歌词支持字号、字体、颜色自定义
- 播放页背景和控件跟随专辑封面主色
- 优化主界面顶部留白和窗口尺寸兼容

## 3.7.0

- 新增设置界面
- 增加 Dock 专辑封面开关

## 3.6.0

- 更换为深蓝紫播放图标
- 侧栏品牌标识同步更新

## 3.5.0

- 首页改为歌词摘要加专辑封面
- 歌词样式可调整
- 主界面到播放页增加封面过渡动画

## 3.0.0

- 新增首页、用户歌单、播放历史
- 新增艺术家详情、排序、播放队列和睡眠定时
