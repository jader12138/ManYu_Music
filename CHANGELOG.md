# 更新日志

## Unreleased

- 歌曲列表多选入口调整：「编辑」按钮从顶部工具栏移到歌曲列表列头「时长」右侧，图标与下方行内爱心居中对齐，只保留勾选圆图标、去掉「编辑」文字；点击后「全选」圆钮出现在列头最左侧、所有行勾选框的正上方（再次点击取消全选），添加到歌单/删除所选两个图标按钮像子菜单一样从编辑图标右侧滑出（浮层不挤压列宽），再点编辑图标即完成退出，不再有单独的取消叉钮，列头文字与歌曲行同步向右移动露出勾选框、不再向左滑动，顶部工具栏不再出现多选胶囊。多选时支持批量「添加到歌单」：操作区文件夹＋图标和歌曲行右键/「⋯」菜单都会把全部选中歌曲一次性加入指定歌单（自动去重），也可直接新建歌单并装入所选歌曲。
- 设置页重构：左侧从 icon rail 改为纯文字侧边栏（蓝色竖线 + 主题色文字选中态），右侧从圆角卡片改为扁平列表行（细分割线 + 左标签右控件）；所有 Toggle 改为 iOS 风格小开关。
- 播放模式三合一与播放页内部队列选歌：播放页控制行原来的「随机播放」「循环」两个按钮改为——一个三态播放模式钮（顺序播放 → 单曲循环 → 随机播放，每点一下切换一个，图标随模式 morph 切换，顺序播放为灰色箭头图标、单曲循环/随机播放时高亮）和一个「列表 ⇄ 对白气泡」融合切换钮（放在原循环钮位置，即上一首左边）。点列表图标后播放页内部的封面+歌词区域整块切换为当前播放队列的选歌列表（不离开播放页，控制栏位置不动、始终在封面下方原位），列表只显示接下来要播的歌曲（当前播放歌曲之后的曲目），水平收窄往中间靠；点任意歌曲立即切换播放，再点气泡图标切回封面+歌词，图标随状态 morph。底部播放条同样改为三态播放模式钮；菜单栏「播放 → 播放模式」同步改为顺序/单曲循环/随机三态选择。
- 歌词居中补偿：前 4 句歌词时自动向上偏移补偿（activeIndex=0 推 36pt，线性衰减到 0），避免刚开始播放时第一句居中导致上方空一大块；随着播放推进自然过渡回标准居中，配合原有逐行 0.7s easeInOut 动画，过渡平滑不突兀。
- 歌词预加载：资料库加载完成后后台批量解析全部曲目的内嵌歌词/LRC 并缓存，切歌播放下一首时也会提前读取队列接下来两曲。现在切换歌曲歌词直接显示，不再先闪“正在等待歌词”占位再跳一下；只有确认没有歌词的歌曲才显示“本歌曲暂无歌词”（加载中显示“正在载入歌词”）。FLAC 歌词读取改为只遍历到 VORBIS_COMMENT 块、seek 跳过 MB 级封面块，批量预热不读图片数据。连续快速切歌（10 首以上）时歌词面板做一次极轻的“呼吸”：整块歌词透明度瞬间下沉再恢复（0.14 秒），不重建歌词视图、零布局开销，重排顿挫被淡入淡出盖住。连续快速切歌时播放器立即暂停（相邻切歌间隔小于 0.7 秒判定为快切），等切歌停顿 0.4 秒后只播放最后选定的最新一首——不再出现“封面、名字、歌词已换，声音和进度条还停在前两首”的错位；慢速点歌行为不变，仍然立即播放。快切期间播放条墙钟插值同步冻结，切歌占用的几秒不再被误记为播放进度，恢复播放后从 0 平滑起步、不再先虚走几秒再跳回 0。

## 3.11.0 - 2026-09-18

- 修复部分 FLAC 歌曲拖到最后一秒后音乐仍继续播放十几秒、歌词与歌声对不齐的问题：播放入口改为开启 AVFoundation 精确时序解析（此前仅资料库扫描开启，播放路径两处遗漏），歌曲现在精确在真实时长处结束；进度显示增加与真实媒体时间的绝对偏差兜底校正（>0.35 秒立即重同步），时钟 tick 投递到主线程的延迟不再逐次累积成进度条超前。
- 长时间听歌内存防护与占用优化：新增内存压力兜底——系统内存吃紧时自动清空可再生成缓存（封面缓存）；「回到顶部」的滚动位置记录表改为弱键结构，页面反复切换不再累积死条目；新增连续切歌 300 次的压力测试守护。占用优化：背景模糊与光斑渲染从 CoreImage 迁移到 vImage/CG 纯 CPU 路径（消除 CoreImage 进程级表面池的逐首滞留）；封面缓存预算 48MB；切歌后自动把空闲内存页归还系统；播放缓冲上限 45 秒；已知时长的曲目不再重复建 AVURLAsset。实测大量快速切歌后驻留内存从约 290MB 降至约 170MB。
- 播放页转场全面丝滑化：封面转场按入口区分动效——播放栏小封面从原位放大成长为大图、主页推荐封面平移就位，退出播放页按原路返回对应封面；换歌时后台 CoreImage 预烘焙全屏模糊背景与两个光斑，转场期间 GPU 不再现算实时模糊层；启动静默期以近乎透明方式预挂载一次完整播放页（含歌词、渐变、光斑、几何配对），首次打开播放页不再有冷启动停顿；歌词面板延迟到转场动画结束后淡入，歌词首次布局不再与转场收尾帧争抢。
- 新增「回到顶部」悬浮按钮：在歌曲页、首页等内容页向下滚得较远后，只要向上滚动一下，右下角就会出现悬浮的胶囊按钮（浮在所有页面之上），点击后平滑滚回列表顶部；向下滚动或接近顶部时自动隐藏。侧栏与播放队列的滚动不会触发。
- Dock 图标不再跳变：应用会记住上一次会话实际使用的图标样式（深色/浅色），下次打开时第一步直接恢复同一图标，不再经过启动早期不可靠的外观解析；若两次会话之间系统/主题外观确实变了，图标才会在启动约 0.8 秒后校正一次。
- 预加载效果修复：封面缓存命中后改为随首帧同步渲染，滚动列表/网格时不再先闪一帧占位图再出封面；预热总量此前超过封面缓存上限导致部分预热结果被挤出，缓存成本上限 128MB→256MB、条目上限 900→1200；预热时已缓存的项目直接快进。
- 新增「启动预加载」：设置 → 资料库里新增开关（默认关闭），开启后软件启动会在后台按显示档位提前把歌曲列表与专辑网格的封面解码进缓存，逐项低优先级进行、不抢占前台；浏览歌曲页、首页、专辑/艺术家页时封面即取即用，不再等待解码。
- 性能优化：进入播放页更顺滑——去掉重复加载的播放页背景（此前同一背景会创建两份全屏模糊封面），封面主色提取与 Dock 图标绘制移出主线程，切歌/进播放页瞬间不再和转场动画抢占主线程。
- 滚动更流畅：滚动条隐藏扫描不再重复写滚动视图属性，消除滚动进行中每 1.5 秒一次的周期性布局顿挫。
- 所有界面不再显示滚动条（含横向），滚动操作本身不受影响，触控板/滚轮照常使用；界面两侧不再有滑块闪现。
- 播放页专辑图下方控件区最右侧新增收藏爱心：点击收藏/取消收藏当前歌曲，收藏后呈实心并高亮；底部播放栏歌曲信息旁同样提供爱心按钮，两处状态实时同步。
- 歌词滚动彻底重构：放弃 ScrollView 程序化滚动（其动画在 macOS 上远距离会直接跳变），改为自管偏移量 + 原生动画——逐行推进 0.7s、点击跳转 1.5s，任何距离都是一条连续缓动滑行；渲染只是平移变换，开销极低。支持拖动浏览歌词，松手后下次换行自动归位。
- 歌词当前行的放大与缩小改为 0.45s easeInOut 渐变过渡，不再瞬间跳大（遵循"减弱动态效果"设置）。
- 歌词左右边距各加宽约一个字的宽度，长行歌词提前换行，不再被视口裁掉半个字。
- 新增歌词进度微调：播放页歌词设置面板（"Aa"按钮）内新增"进度"行，按 0.2 秒步进把歌词整体提前或延后，实时生效并按歌曲自动记忆（上限 ±10 秒）；重置按钮常驻，重置后仅置灰、面板不跳动。
- 歌词解析过滤"制作人员名单"行（词：/曲：/编曲：/混音：/OP： 等）与平台版权声明行：这类行以 0.1 秒间隔密集挤在歌词开头，此前会被当成歌词疯狂滚过，造成歌词与歌曲对不齐的观感；现在正文从第一句唱词开始显示。
- 播放页控制行"喜欢"右侧新增小喇叭音量键：点击弹出横向滑块（可点击轨道或拖动小球调整音量），点击滑块与喇叭以外的任意位置自动收起、且那次点击本身的功能照常执行；滑块展开期间关闭悬停滚轮调节；收起时悬停喇叭上下滚动可调音量，图标下方小字实时显示音量数字。
- 播放页歌曲标题改用专辑封面主色与白色的混合渐变（浅色模式下文字端自动换成深色保证可读），切歌时颜色平滑过渡。
- 播放页背景渐变动起来：主渐变以 90 秒一个来回极慢摆动、两个光斑以 55-68 秒缓慢漂移，营造缓慢流动的氛围（遵循"减弱动态效果"设置）。
- 播放模式互斥：开启随机播放自动退出循环模式，进入循环（含单曲循环）自动关闭随机；随机/循环移到"上一曲"左侧，选中时按钮本身不再亮起，改为图标下方主题色小点（顺序播放无点）。
- 经全库扫描确认：370 首 FLAC 内嵌歌词来源统一、无 .lrc 冲突、无时间戳精度问题；个别歌曲与母带音源的整体时间差可用"歌词进度微调"按歌校准并自动记忆。
- 进度条彻底重构：时钟 tick 送达时间天然抖动，此前任何"tick 触发动画"的方案都会一跳一跳；现改为记录 tick 锚点后由 TimelineView 按帧插值（30fps），显示进度与 tick 到达时刻完全无关，天然连续；拖动小球实时跟手。
- 修复点击歌词/进度条跳转后进度显示与时钟"乒乓"冲突的问题：seek 落位完成前丢弃观察器的旧位置回调，跳转后进度条不再回跳抖动。

- 窗口左上角的红黄绿三个系统按钮整体往右、往下微调，不再紧贴窗口左上角。
- 首页快捷操作顺序调整为："继续播放"在最前，其后依次是"播放全部""随机播放"，三个按钮使用同一套悬停动效。
- "继续播放"跨启动记忆说明：设置 → 播放记忆（默认开启）会保存关闭软件时的歌曲、队列与进度；重新打开后点"继续播放"即从上次位置接着播。

- 歌曲列表排序改为点击列头完成：点击"标题"按歌名字母序、"专辑"按专辑名字母序、"时长"按时长排列；再点同一列头切换升/降序。激活列头右侧显示深色小尖角（升序向上、降序向下），列头文字位置和颜色不受影响。
- 歌曲页搜索框左侧的原排序按钮已移除，其功能融合进"标题 / 专辑 / 时长"三个列头；未点击列头时保持原有的添加时间顺序。

- 界面风格向椒盐音乐式的扁平简洁靠拢：侧栏去掉渐变玻璃底改为纯色平面，选中项改为主题色浅色圆角块（移除左侧竖条）；底部播放条改为实色平面。首页"推荐曲目"卡片保留原有玻璃质感。
- 新增统一设计令牌：`HPMetrics` 圆角层级（行 10 / 卡片 16 / 瓦片 20）与 `Color.hpHairline` 发丝线，后续视图统一引用。
- 全应用交互动效升级（对齐 Apple Music 风格）：默认无背景的按钮悬停时以圆角矩形渐显主题色高亮，按下加深；主操作按钮悬停轻微提亮上浮。侧栏导航、设置页签、详情页工具按钮、显示切换器、歌词样式按钮均已接入。
- 所有交互按钮取消椭圆（胶囊）造型，统一改为圆角矩形；主操作按钮为实色圆角矩形。
- "最近添加"列表模式去除歌曲行下的常驻灰底，改为悬停时才显示高亮，与底色更融合。
- "最近添加"改为按歌曲展示：卡片显示歌曲名与演唱者（不再显示专辑名），封面调小、歌名与演唱者更紧凑。
- "最近添加"右上角新增显示方式切换：封面网格 / 横向文件名列表，选择会记住。
- 首页改版：移除"最近播放"横滑区块，"最近添加"改为纵向平铺网格（每行约 5~6 张卡片，随窗口宽度自适应，不再横向滚动）。
- 首页快捷操作新增"继续播放"按钮（恢复上次播放进度）。
- 切换白天/夜间模式时，左上角品牌图标同步切换：白天显示白色图标，夜间显示深色图标，并带淡入过渡（遵循"减弱动态效果"设置）。
- 左上角品牌图标不再受设置中"应用图标"风格固定的限制；该设置现在只影响 Dock 图标。
- 修复"每次打开软件时更新"的首页推荐在切换页面后被重新随机的问题：推荐现在在整个运行期间保持稳定，重启应用后才更换。
- 设置中的推荐频率选项更名为"每次打开软件时更新 / 每天更新"，并说明切换页面时推荐保持不变。
- 大曲库浏览更流畅：列表、专辑、歌手、文件夹的分组与搜索改为后台预计算，一次事务刷新界面。
- 封面按显示尺寸三级降采样加载，长列表滚动更快、内存更低；小尺寸缩略图不再叠加投影。
- 播放进度与睡眠定时器倒计时独立刷新，不再触发整个界面重算。
- 播放/暂停图标与详情页切换动画遵循系统"减弱动态效果"设置。
- 新增 20 个单元测试，覆盖资料库持久化、浏览快照、封面管线与播放时钟隔离。

## 3.10.0 - 2026-09-13

- 播放页窗口标题栏改为完全透明，顶部不再出现独立色带；返回按钮改为左上角圆角 V 形，并补充随机、循环控制。
- 播放页布局按窗口尺寸伸缩，减少底部留白；歌词样式面板增加行距和显示行数。
- 调整开发流程为一功能一分支，合并和发布完成后删除已用完的分支。
- 增加功能完成或成功合并后的固定结束语规则。

## 3.9.0 - 2026-09-13

- 新增按版本独立保存的发布记录、历史版本归档和发布模板。
- 修复 README 被源码内容覆盖的问题，恢复项目说明并补充文档索引。
- 放大播放主界面左侧封面与下方控制条，并优化歌词渐隐和当前行强调效果。
- 调整 Dock 专辑封面留白，缩小封面并扩大亚克力背板的可视边框。
- 设置中新增“记住上次播放状态”，重启后恢复歌曲、队列、进度和音量。
- 启动焦点默认落在播放按钮，未编辑搜索框时按空格可播放或暂停。
- 修复歌曲列表顶部大块空白，列表首行紧跟表头。
- 首页“最近播放”在当前停留期间保持顺序，不再随切歌跳动。
- Dock 专辑封面增加窄幅玻璃背板，兼容不同尺寸封面并遮挡白边。
- 顶部“漫域音乐”品牌和图标支持点击返回首页。
- 切歌时保留上一张封面，消除 Dock 图标闪回默认图标。
- 设置界面改为与主界面一致的白蓝玻璃卡片。
- 设置界面改为主窗口内嵌页面，不再打开独立设置窗口。
- 应用菜单和快捷键设置入口改为切换到主窗口的设置页面。
- FLAC 等格式标签移动到歌曲信息右侧。
- Dock 专辑封面角标改为封面取色的玻璃亚克力样式，并放大播放/暂停符号。
- FLAC 等格式标签移动到歌曲名和歌手右侧。
- 设置入口移动到左侧栏底部。
- 主界面移除“导入”和“添加音乐”按钮，音乐导入统一放入设置。
- 底部播放栏缩为 72pt，并缩小专辑封面和播放按钮。
- 首页歌词摘要固定展示 4 行。
- 首页播放按钮移入专辑封面内部，并根据封面主色自动配色。
- 首页副标题只显示歌曲总数，不再显示专辑数量。
- 首页歌词竖线与文本严格等高，避免底部空白。
- 首页专辑封面增加玻璃材质衬托层。
- 播放状态按钮改为无白色描边的玻璃亚克力样式。
- 首页歌词摘要改为优先截取歌词中段，并过滤制作人员信息。
- 首页专辑封面支持点击进入完整播放界面，右下角状态按钮支持播放/暂停。
- 首页问候语增加中文诗句、简短问候和英文短句轮换。
- 增加深色、浅色两套 App 图标资源。
- 设置中增加“自动 / 深色 / 浅色”三档 App 图标选择。
- Dock 基础图标和应用侧栏图标会跟随图标设置。
- Dock 专辑封面保留为播放设置中的独立开关，不放入图标选择。
- 修复侧栏品牌图标与 App 图标不一致。
- 增加强制分支开发、合并提醒和功能文档规则。

本项目使用语义化版本号：`主版本.次版本.修订版本`。

## 3.8.0 - 2026-09-13

- 修复 macOS Dock 图标白边和底部裁切问题
- 重新生成透明圆角、多尺寸 AppIcon
- 新增设置窗口，可从侧边栏齿轮或系统设置菜单进入
- 新增播放时在 Dock 显示专辑封面
- Dock 专辑封面增加白色播放/暂停状态角标
- 首页推荐歌词改为连续显示 3～4 行
- 歌曲列表和首页封面右下角统一使用自适应播放状态图标
- 修复歌词页控件底色突兀、蓝色光晕和标题栏冗余问题
- 歌词支持字号、字体、颜色自定义
- 播放页背景和控件跟随专辑封面主色
- 优化主界面顶部留白和窗口尺寸兼容

## 3.7.0

- 新增设置界面
- 增加 Dock 专辑封面开关

## 3.6.0

- 更换为深蓝紫播放图标
- 侧栏品牌标识同步更新

## 3.5.0

- 首页改为歌词摘要加专辑封面
- 歌词样式可调整
- 主界面到播放页增加封面过渡动画

## 3.0.0

- 新增首页、用户歌单、播放历史
- 新增艺术家详情、排序、播放队列和睡眠定时
