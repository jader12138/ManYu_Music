# 更新日志

## Unreleased（3.13.0 正式版准备中）

### 后台播放（分支 codex/background-playback）

#### 新增（关闭窗口后继续后台播放）

- 设置 → 播放新增「关闭窗口后继续后台播放」开关（默认关闭）。开启后点击窗口红叉不再退出应用，音乐继续在后台播放；点击 Dock 图标可重新打开主窗口。实现方式：`applicationShouldTerminateAfterLastWindowClosed` 根据 UserDefaults 键 `ManyuMusic.keepPlayingAfterWindowClose` 返回布尔值；新增 `applicationShouldHandleReopen` 在无可见窗口时重新唤出主窗口。
- 底部播放条右侧工具区新增「歌词」开关按钮（`text.alignleft` 图标）：一键显示/隐藏菜单栏实时歌词，开启时按钮高亮，状态自动持久化（默认显示），重启后保持。

#### 改进（菜单栏迷你播放器打磨）

- 迷你播放器进度条滑块从空心圆环改为实心蓝色圆点并加柔和阴影；非拖动时进度推进带 0.25 秒缓出动画，拖动时严格跟手且松手才跳转，避免拖动过程中频繁 seek 造成卡顿。
- 迷你播放器播放/暂停、上一首、下一首按钮新增按压缩放回弹动画（缩放至 0.88 后弹回，0.19 秒）。

### 系统集成（分支 codex/dock-menu）

#### 新增（菜单栏实时歌词与 Dock 右键菜单）

- 菜单栏实时歌词与播放控制：系统菜单栏常驻两个相邻状态项——左侧固定宽度（160pt）歌词区实时显示当前歌词、随播放逐句更新（纯展示，点击无高亮、无底色反应，歌词间隙沿用上一句避免闪烁，无歌词时整区隐藏），右侧为播放图标（主人设计的任务栏图标，模板图自适配深浅色菜单栏）；左键或右键点击图标弹出「播放 / 暂停」菜单，两项按当前播放状态启用。设置 → 播放新增「菜单栏播放控制」开关（默认开启，即时生效）。
- Dock 图标右键菜单：显示当前歌曲名（加粗）与歌手·专辑（次色），以及播放/暂停（文案与图标随播放状态切换）、上一首、下一首三项命令；无曲目时显示「未在播放」占位且命令置灰。每次右键实时取当前状态。
- Dock 右键菜单顶部不再显示系统自动附加的窗口标题项（应用名 + 对勾）：主窗口已排除出窗口列表。
- 两处入口均不依赖主窗口：播放/暂停等操作直接作用于全局播放器。

#### 修复（菜单栏歌词底色与图标形状，分支 codex/menubar-icon-fix）

- 歌词区不再有黑色背景板：放弃 `NSStatusItem.view` 自定义视图方案（当前 macOS 版本下设 view 后状态项完全不可见），改用 `NSStatusItem.button` 并设置 `isBordered = false`、`showsBorderOnlyWhileMouseInside = false` 消除边框和点击高亮，`contentTintColor = .labelColor` 跟随菜单栏深浅色自动切换，与亚克力背景完全融合、零底色、点击零反应。
- 状态栏状态项延迟创建：启动早期菜单栏尚未完成布局，过早注册状态项会被系统静默忽略；改为 `DispatchQueue.main.asyncAfter` 延迟 3 秒创建，确保状态项稳定出现。
- 菜单栏图标恢复为主人设计的原样：此前 SVG 转 Swift 时曲线控制点漏做 y 轴翻转，导致图标形状完全走样；现已修正并重新生成，图标与原始设计一致（模板图，深浅色自动反色）。

## v3.13.0-beta4（2026-09-20，内部测试版）

### 均衡器与音效（本版重点）

#### 新增（31 段均衡器，分支 codex/equalizer）

- 均衡器页面全面重做（参考 MoeKoe EQ 插件设计）：
  - 规格升级为 31 段参数均衡器：20Hz ~ 20kHz（ISO 三分之一倍频程），每段增益 ±6dB（0.5 步进）、Q 值独立可调（0.1~18，默认 1.4，点击各段 Q 值弹出编辑滑杆）；旧版十段曲线与自定义预设自动插值迁移，不丢数据。
  - 头部状态栏：「31段均衡器」标题、EQ 运行状态指示灯（绿点）、关闭EQ/开启EQ 按钮、红色「重置」按钮（全部归零 + Q 恢复默认）；弹窗场景带关闭按钮。
  - 三页签：均衡器 / 音效增强 / 高级功能；「高级功能」页签本轮为占位，后续版本实现。
  - 「音效增强」页签完整实现（独立开关 + 区内重置，全部实时生效并自动持久化）：
    - 频率调节 8 项：低频提升（60Hz 低架）、低音增强（100Hz）、温暖感（250Hz）、人声增强（3kHz）、临场感（4kHz）、清晰度（8kHz）、高频提升（8kHz 高架）、动态增强（压低响亮段落峰值的压缩器）；
    - 空间效果 3 项：环境感（早反射空间感）、环绕声（M/S 立体声扩展）、环境混响（Schroeder 混响尾，左右去相关）；
    - 输出控制 2 项：输出增益（-12 ~ +12 dB）、声道平衡（左/居中/右）。
    - DSP 在音频 tap 内于 EQ 之后级联，与 EQ 开关互相独立；预分配缓冲，实时线程无内存分配。
  - 「频谱分析」实时频谱：播放中的音乐经常驻音频 tap 下混采样，30fps FFT 绘制绿色频谱线（含渐变填充与频率刻度），暂停时谱线平滑归零；EQ 开关不影响频谱。频谱观感本轮重构为「山峰」风格（分支 codex/spectrum-peaks）：对数频轴采样从 64 点加密到 384 点，频谱面板高度提升一半（120→180pt）且水平参考线加密为五等分；修复 FFT 幅度未归一化导致的「平顶高原」——输出乘 2/N 恢复真实幅度后按 dB 归一化窗口 -70~0dB 映射，叠加高频倾斜补偿（+8dB 对数 tilt）与幂次对比拉伸（^1.5），峰更突出、谷更深、高低错落；曲线改为弱张力 Catmull-Rom 保留陡峭峰形，移除人工水波基线与相位回声波，改为跟随主线的矮化「远山影」第二层轮廓，静默/暂停时衰减贴底。
  - 预设区改为 chips 流式排布：内置 35 个预设曲线（平直/摇滚/古典/流行/爵士/低音增强/高音增强/人声/仿：风雪调音/极致听感/醇美空间/哈基米曲线/母带处理/黑胶温暖/Hi-Res解析/演唱会近场/沉浸全景/大师监听/电子舞曲/嘻哈说唱/民谣原声/播客有声书/影院模式/Lo-Fi休闲/金属硬核/ASMR助眠/运动/古典钢琴/澎湃重低音/通透饱满/华语流行/欧美流行/R&B律动/声场/派大星，曲线来自参考项目）+ 自定义预设 + 「+ 保存预设」。
  - 均衡器调节区：31 根垂直滑杆，中心 0dB 基线，双击滑杆归零，每段下方显示增益与 Q 值、底部斜排频率标签。
- 播放栏快捷图标（「定时」左侧）弹出精简面板：仅保留头部（标题/状态灯/开关EQ/重置/关闭）与预设 chips，点选预设即生效；设置独立「均衡器」页保留完整面板（页签/频谱/滑杆）。原「播放」页中的均衡器区块已移除。
- 实现说明：音频处理 tap 改为常驻所有播放条目（EQ 关闭时直通、频谱照常取流），MTAudioProcessingTap + RBJ biquad（两端 shelf、中间 peaking）级联滤波；频谱用 Accelerate/vDSP（Hann 加窗 + 实数 FFT + 对数频轴聚合），纯本地计算。

#### 新增（无缝播放与淡入淡出，分支 codex/gapless-crossfade）

- 设置 → 播放新增「无缝播放（Gapless）」开关：开启后一首歌自然播到结尾前会提前在备用播放引擎预载并预热（preroll）下一首，在结尾处近无间隙接管，整张专辑连续听曲间不再有明显停顿。默认关闭。
- 设置 → 播放新增「淡入淡出（Crossfade）」开关与「淡入淡出时长」滑块（3~12 秒，默认 6 秒）：开启后切歌（手动点上一首/下一首/选歌，以及自动连播）时上一首渐弱、下一首渐强、两首短暂重叠过渡，smoothstep 缓动手感更自然。暂停时切歌、连续快切、首播仍立即切换不走淡变；拖动进度、暂停、改播放模式、增删队列都会立刻撤销未完成的过渡。crossfade 与 gapless 同时开启时自动连播以 crossfade 为准。默认关闭。
- 单曲循环、列表结束停止、随机播放等既有模式行为保持不变；两个开关都关闭时播放链路与历史完全一致。

### 外观与图标

#### 新增（Dock 徽标与播放页切歌转场，分支 codex/dock-badge-crossfade）

- Dock 专辑封面模式右下角的播放/暂停蓝白徽标往左上内收，完全退入亚克力底板，不再骑边、压边。
- 播放页专辑封面切歌时新增方向性过渡：新封面从旧封面「后面」顶出（轻微上浮放大淡入，层级在后），旧封面向左滑出并淡出消散；用固定尺寸 ZStack 叠加，标题与歌词等周围布局不再被顶动，遵循系统「减弱动态效果」设置。

#### 新增（亚克力包边与图标尺寸标准化，分支 codex/artwork-acrylic-border + codex/icon-size-standard）

- 底部播放条左下角专辑封面外包裹一圈亚克力包边：与首页「推荐曲目」hero 封面同款玻璃材质（`ultraThinMaterial` + 白色微光），按小封面等比缩小（外框 54pt / 封面 48pt），包边沿 0.6pt 灰色发丝线并带轻阴影，白色系封面也能清楚看到边界；包边不参与进播放页的放大转场。
- Dock 图标与启动台图标尺寸对齐 macOS 标准网格：白色/黑色静态图标内容占比从 87.5% 缩到约 80.5%（1024 母版四周留 10% 透明边，PNG 与 icns 全部重新生成）；专辑封面模式的 Dock 图标（含播放/暂停徽标）整体等比缩到同一占比，三种 Dock 状态（白色/黑色/专辑封面）视觉尺寸完全一致，与系统其他应用图标同档。

### 验证

- `swift test --disable-sandbox`：81/81 通过（均衡器/音效增强/连播增强 24 项 + 频谱分布 1 项 + 既有回归 56 项）；release 构建通过。
- 实机验收：实时频谱呈高低错落山峰形态；均衡器预设/滑杆、音效增强 13 项、Gapless/Crossfade 链路均工作正常，默认关闭不影响既有播放行为。听感细节继续收集内测反馈。

## v3.13.0-beta3（2026-09-20，内部测试版）

### 外观与默认主题

- 软件默认主题从「夜间模式」改为「跟随系统」：首次安装时系统为浅色则软件呈白色（白天）模式，系统为深色则呈夜间模式；用户仍可在设置 → 外观里手动固定白天/夜间。
- DMG 安装镜像与 Finder 里的 .app 图标固定为白色版（`AppIcon` 对齐 `AppIconLight`）：打包安装只进行一次，DMG 卷宗图标永远白色，不随系统外观变化。
- Dock 栏图标与软件界面跟随系统外观在白/黑版本间切换（沿用既有 automatic 逻辑）。

### 收藏与格式标识

- 「我喜欢」爱心选中后变红色：播放页控制行、底部播放条的收藏爱心从主题蓝改为玫红色（`hpPink`），与歌曲列表爱心颜色统一；未收藏仍为灰色描边。
- 文件格式徽标按格式显示不同颜色：FLAC 金色、MP3 蓝色、MP4/M4A/MOV 等家族淡红色，WAV 青绿、AIFF 橙、OGG 绿、OPUS 紫、AAC 紫罗兰、WMA 棕橙、ALAC 麦金，未识别格式回退灰色；徽标背景用同色低透明度。底部播放条、歌曲列表、歌曲信息详情页三处统一。
- 底部播放条格式徽标位置调整：从右侧远距移到歌名正后方并略微下移，视觉居中于歌名与歌手名两行之间。

### 播放页与播放条

- 主页底部播放条整条空白区域均可点击进入播放页：除播放/暂停、循环模式、上一首/下一首、收藏、音量、睡眠定时、队列等已有具体功能的控件以外，曲目区、Spacer、padding 与进度条之外的空白处点击都会触发进入播放页；封面按钮原入口保留不变。
- 播放页底部渐变色效果调整：
  - 颜色更重：深色模式主色不透明度 0.58 → 0.82，副色 0.34 → 0.55，新增第三色（accent）层 0.46；浅色模式相应加深；模糊背景图层与两枚光斑 opacity 同步上调。
  - 抽取的颜色样本更多：封面缩略图改为按 3×3 网格分块，每块独立做饱和度加权平均得到区域代表色；`secondary` 与 `accent` 不再只是 `primary` 旋转色相派生，而是从九宫格里挑选与 `primary` 色相距离最大、饱和度合理的真实区域色（单色封面回退派生保持兼容）。
  - 渐变色动起来：动画 duration 从 40~90 秒缩到 9~16 秒，肉眼可见；各层节奏错相（9/11/13/16s）配合偏移与不对称角度，背景扫动不再呈现机械的左右对称。
  - 设置 → 播放新增「播放页背景动画」开关：关闭时背景渐变完全静止；开启时按上述节奏缓慢扫动（reduceMotion 用户始终关闭，开关无效）。
- 底部播放条曲目区整体微调：封面 44→48pt、圆角改为与播放页同观感，歌名 11→12pt、歌手名 10→11pt，曲目区整体右移 20pt。
- 全应用专辑封面圆角统一：新增 `ArtworkLayout.cornerRadius(for:)`，除播放页大封面（保持 22pt）与艺术家圆形头像外，首页推荐/最近添加（网格与列表）、歌曲列表、专辑卡片、专辑详情、歌单详情、底部播放条的封面全部按 7:48 比例取圆角，悬停遮罩与空态占位同步。

### 修复

- 修复单元测试崩溃：`AppIconStyleManager` 在未初始化 `NSApplication` 的进程（测试宿主）里直接解包 `NSApp.effectiveAppearance` 导致 fatal error；改为可选链安全读取，无应用实例时按默认浅色处理。

## v3.13.0-beta2（2026-09-20，内部测试版）

### 修复

- 修复重新打开软件后底部播放栏为空、不恢复上次曲目与播放队列的问题：歌库从磁盘异步加载，而播放状态恢复只挂在主界面首次出现时（此刻库尚未加载完，恢复从不执行）；现改为在歌库加载完成时恢复，并保证库为空时不消耗唯一一次恢复机会。
- 修复窗口宽度在 840～1040pt 时底部播放条"变形"的问题：窄屏布局把曲目区设为无限宽，导致格式徽标被推到窗口中部、播放控件偏在右侧；改为与宽屏同构的定宽曲目区 + 居中控件。
- 窗口大小记忆恢复时增加最小尺寸钳制：存档窗口小于 840×520 时自动放大到合法尺寸并居中（原来只处理超屏收缩）。
- 内嵌歌词识别扩展到所有支持的音频格式：MP3 读取 ID3v2 的 USLT 帧（支持 ID3v2.3/2.4、UTF-8/UTF-16/ISO-8859-1 文本编码），MP4/M4A/M4B/MOV 读取 `moov.udta` 路径下的 `©lyr` atom。此前只有 FLAC 的 Vorbis LYRICS 能被自家解析识别，MP3/MP4 的内嵌歌词需依赖 AVFoundation 兜底，实际经常读不到；现在与 FLAC 同级的自家二进制解析在 AVFoundation 之前生效，软件支持什么格式就识别什么格式的内嵌歌词。外置 `.lrc` 维持不变。

### 内部改进

- `AudioPlayer` 支持注入独立 UserDefaults（生产环境仍使用标准偏好），播放状态恢复测试不再读写真实用户偏好；新增 3 个恢复时序单元测试。
- 新增 5 个单元测试覆盖 MP3（ID3v2.3/2.4、UTF-8/UTF-16-BOM）、MP4（m4a/mp4 扩展名、有/无 ©lyr）、不支持格式的分发兜底。

## v3.13.0-beta1（2026-09-18，内部测试版）

- 设置 → 资料库的「不扫描 60 秒以下的音频」开关现在会持久化保存，重启软件后保持上次的选择（此前每次启动都恢复为关闭）。
- 窗口大小与位置自动记忆：调整过的窗口尺寸和摆放位置在下次打开软件时恢复，首次启动仍使用默认 1080×650 居中窗口。
- 首次使用引导优化：资料库为空时，首页不再显示全部禁用的快捷操作；空状态按钮由「前往设置添加音乐」改为「添加音乐文件夹」，点击直接弹出文件夹/文件选择面板，并提示也可以把音乐文件直接拖进窗口。
- 设置新增「关于」标签页：显示应用图标、完整版本号与内部构建号、内部测试版标识、适用系统与版权信息；内部测试版提供「在 GitHub 上反馈问题」按钮直达 Issues 页面。
- 内测打包支持：`VERSION` 允许 `3.13.0-beta1` 形式的预发布后缀（系统版本号仍为纯数字三段，完整版本号写入包内供界面读取）。
- 更正 v3.12.0 发布说明：当前构建仅支持 Apple 芯片（arm64），不支持 Intel Mac。

## 3.12.0 - 2026-09-18

- 首个公开 DMG 安装镜像：在 GitHub Releases 发布 `ManyuMusic-3.12.0.dmg`，挂载后将「漫域音乐」拖入应用程序即可使用。
- 修复在设置中移除音乐文件夹后歌曲数量不减少的问题：音乐来源现在会持久化到资料库，重启后仍然显示；移除某个来源时会立即删除该文件夹（或文件）下的全部歌曲，并同步清理这些歌曲的收藏、播放历史与歌单引用；即使在「添加文件夹」后的扫描过程中立即移除，在途扫描结果也不会再把歌曲重新加回来。路径比较统一做标准化和符号链接解析，软链/别名形式选中的文件夹也能正确匹配。
- 设置 → 资料库新增「音乐来源」管理：通过面板或拖入添加的文件夹/文件都会列入清单（显示名称与完整路径、文件夹与单曲图标区分），可逐项点「−」移除并连带移除其歌曲；新增「不扫描短音频」开关（开启后导入时自动过滤时长不足 60 秒的音频）与「屏蔽文件夹」清单（扫描时跳过指定子目录，可随时取消屏蔽）；来源与屏蔽清单均随 library.json 持久化。
- 歌曲列表多选入口调整：「编辑」按钮从顶部工具栏移到歌曲列表列头「时长」右侧，图标与下方行内爱心居中对齐，只保留勾选圆图标、去掉「编辑」文字；点击后「全选」圆钮出现在列头最左侧、所有行勾选框的正上方（再次点击取消全选），添加到歌单/删除所选两个图标按钮像子菜单一样从编辑图标右侧滑出（浮层不挤压列宽），再点编辑图标即完成退出，不再有单独的取消叉钮，列头文字与歌曲行同步向右移动露出勾选框、不再向左滑动，顶部工具栏不再出现多选胶囊。多选时支持批量「添加到歌单」：操作区文件夹＋图标和歌曲行右键/「⋯」菜单都会把全部选中歌曲一次性加入指定歌单（自动去重），也可直接新建歌单并装入所选歌曲。时长列改为左对齐并整体左移 6pt，列头「时长」与每行时间严格同位对齐；多选时编辑图标与滑出的添加歌单、删除图标三者间距等距拉大。
- 设置页重构：左侧从 icon rail 改为纯文字侧边栏（蓝色竖线 + 主题色文字选中态），右侧从圆角卡片改为扁平列表行（细分割线 + 左标签右控件）；所有 Toggle 改为 iOS 风格小开关。
- 播放模式三合一与播放页内部队列选歌：播放页控制行原来的「随机播放」「循环」两个按钮改为——一个三态播放模式钮（顺序播放 → 单曲循环 → 随机播放，每点一下切换一个，图标随模式 morph 切换，顺序播放为灰色箭头图标、单曲循环/随机播放时高亮）和一个「列表 ⇄ 对白气泡」融合切换钮（放在原循环钮位置，即上一首左边）。点列表图标后播放页内部的封面+歌词区域整块切换为当前播放队列的选歌列表（不离开播放页，控制栏位置不动、始终在封面下方原位），列表只显示接下来要播的歌曲（当前播放歌曲之后的曲目），水平收窄往中间靠；点任意歌曲立即切换播放，再点气泡图标切回封面+歌词，图标随状态 morph。底部播放条同样改为三态播放模式钮；菜单栏「播放 → 播放模式」同步改为顺序/单曲循环/随机三态选择。
- 歌词居中补偿：前 4 句歌词时自动向上偏移补偿（activeIndex=0 推 36pt，线性衰减到 0），避免刚开始播放时第一句居中导致上方空一大块；随着播放推进自然过渡回标准居中，配合原有逐行 0.7s easeInOut 动画，过渡平滑不突兀。
- 歌词预加载：资料库加载完成后后台批量解析全部曲目的内嵌歌词/LRC 并缓存，切歌播放下一首时也会提前读取队列接下来两曲。现在切换歌曲歌词直接显示，不再先闪“正在等待歌词”占位再跳一下；只有确认没有歌词的歌曲才显示“本歌曲暂无歌词”（加载中显示“正在载入歌词”）。FLAC 歌词读取改为只遍历到 VORBIS_COMMENT 块、seek 跳过 MB 级封面块，批量预热不读图片数据。连续快速切歌（10 首以上）时歌词面板做一次极轻的“呼吸”：整块歌词透明度瞬间下沉再恢复（0.14 秒），不重建歌词视图、零布局开销，重排顿挫被淡入淡出盖住。连续快速切歌时播放器立即暂停（相邻切歌间隔小于 0.7 秒判定为快切），等切歌停顿 0.4 秒后只播放最后选定的最新一首——不再出现“封面、名字、歌词已换，声音和进度条还停在前两首”的错位；慢速点歌行为不变，仍然立即播放。快切期间播放条墙钟插值同步冻结，切歌占用的几秒不再被误记为播放进度，恢复播放后从 0 平滑起步、不再先虚走几秒再跳回 0。

## 3.11.0 - 2026-09-18

- 修复部分 FLAC 歌曲拖到最后一秒后音乐仍继续播放十几秒、歌词与歌声对不齐的问题：播放入口改为开启 AVFoundation 精确时序解析（此前仅资料库扫描开启，播放路径两处遗漏），歌曲现在精确在真实时长处结束；进度显示增加与真实媒体时间的绝对偏差兜底校正（>0.35 秒立即重同步），时钟 tick 投递到主线程的延迟不再逐次累积成进度条超前。
- 长时间听歌内存防护与占用优化：新增内存压力兜底——系统内存吃紧时自动清空可再生成缓存（封面缓存）；「回到顶部」的滚动位置记录表改为弱键结构，页面反复切换不再累积死条目；新增连续切歌 300 次的压力测试守护。占用优化：背景模糊与光斑渲染从 CoreImage 迁移到 vImage/CG 纯 CPU 路径（消除 CoreImage 进程级表面池的逐首滞留）；封面缓存预算 48MB；切歌后自动把空闲内存页归还系统；播放缓冲上限 45 秒；已知时长的曲目不再重复建 AVURLAsset。实测大量快速切歌后驻留内存从约 290MB 降至约 170MB。
- 播放页转场全面丝滑化：封面转场按入口区分动效——播放栏小封面从原位放大成长为大图、主页推荐封面平移就位，退出播放页按原路返回对应封面；换歌时后台 CoreImage 预烘焙全屏模糊背景与两个光斑，转场期间 GPU 不再现算实时模糊层；启动静默期以近乎透明方式预挂载一次完整播放页（含歌词、渐变、光斑、几何配对），首次打开播放页不再有冷启动停顿；歌词面板延迟到转场动画结束后淡入，歌词首次布局不再与转场收尾帧争抢。
- 新增「回到顶部」悬浮按钮：在歌曲页、首页等内容页向下滚得较远后，只要向上滚动一下，右下角就会出现悬浮的胶囊按钮（浮在所有页面之上），点击后平滑滚回列表顶部；向下滚动或接近顶部时自动隐藏。侧栏与播放队列的滚动不会触发。
- Dock 图标不再跳变：应用会记住上一次会话实际使用的图标样式（深色/浅色），下次打开时第一步直接恢复同一图标，不再经过启动早期不可靠的外观解析；若两次会话之间系统/主题外观确实变了，图标才会在启动约 0.8 秒后校正一次。
- 预加载效果修复：封面缓存命中后改为随首帧同步渲染，滚动列表/网格时不再先闪一帧占位图再出封面；预热总量此前超过封面缓存上限导致部分预热结果被挤出，缓存成本上限 128MB→256MB、条目上限 900→1200；预热时已缓存的项目直接快进。
- 新增「启动预加载」：设置 → 资料库里新增开关（默认关闭），开启后软件启动会在后台按显示档位提前把歌曲列表与专辑网格的封面解码进缓存，逐项低优先级进行、不抢占前台；浏览歌曲页、首页、专辑/艺术家页时封面即取即用，不再等待解码。
- 性能优化：进入播放页更顺滑——去掉重复加载的播放页背景（此前同一背景会创建两份全屏模糊封面），封面主色提取与 Dock 图标绘制移出主线程，切歌/进播放页瞬间不再和转场动画抢占主线程。
- 滚动更流畅：滚动条隐藏扫描不再重复写滚动视图属性，消除滚动进行中每 1.5 秒一次的周期性布局顿挫。
- 所有界面不再显示滚动条（含横向），滚动操作本身不受影响，触控板/滚轮照常使用；界面两侧不再有滑块闪现。
- 播放页专辑图下方控件区最右侧新增收藏爱心：点击收藏/取消收藏当前歌曲，收藏后呈实心并高亮；底部播放栏歌曲信息旁同样提供爱心按钮，两处状态实时同步。
- 歌词滚动彻底重构：放弃 ScrollView 程序化滚动（其动画在 macOS 上远距离会直接跳变），改为自管偏移量 + 原生动画——逐行推进 0.7s、点击跳转 1.5s，任何距离都是一条连续缓动滑行；渲染只是平移变换，开销极低。支持拖动浏览歌词，松手后下次换行自动归位。
- 歌词当前行的放大与缩小改为 0.45s easeInOut 渐变过渡，不再瞬间跳大（遵循"减弱动态效果"设置）。
- 歌词左右边距各加宽约一个字的宽度，长行歌词提前换行，不再被视口裁掉半个字。
- 新增歌词进度微调：播放页歌词设置面板（"Aa"按钮）内新增"进度"行，按 0.2 秒步进把歌词整体提前或延后，实时生效并按歌曲自动记忆（上限 ±10 秒）；重置按钮常驻，重置后仅置灰、面板不跳动。
- 歌词解析过滤"制作人员名单"行（词：/曲：/编曲：/混音：/OP： 等）与平台版权声明行：这类行以 0.1 秒间隔密集挤在歌词开头，此前会被当成歌词疯狂滚过，造成歌词与歌曲对不齐的观感；现在正文从第一句唱词开始显示。
- 播放页控制行"喜欢"右侧新增小喇叭音量键：点击弹出横向滑块（可点击轨道或拖动小球调整音量），点击滑块与喇叭以外的任意位置自动收起、且那次点击本身的功能照常执行；滑块展开期间关闭悬停滚轮调节；收起时悬停喇叭上下滚动可调音量，图标下方小字实时显示音量数字。
- 播放页歌曲标题改用专辑封面主色与白色的混合渐变（浅色模式下文字端自动换成深色保证可读），切歌时颜色平滑过渡。
- 播放页背景渐变动起来：主渐变以 90 秒一个来回极慢摆动、两个光斑以 55-68 秒缓慢漂移，营造缓慢流动的氛围（遵循"减弱动态效果"设置）。
- 播放模式互斥：开启随机播放自动退出循环模式，进入循环（含单曲循环）自动关闭随机；随机/循环移到"上一曲"左侧，选中时按钮本身不再亮起，改为图标下方主题色小点（顺序播放无点）。
- 经全库扫描确认：370 首 FLAC 内嵌歌词来源统一、无 .lrc 冲突、无时间戳精度问题；个别歌曲与母带音源的整体时间差可用"歌词进度微调"按歌校准并自动记忆。
- 进度条彻底重构：时钟 tick 送达时间天然抖动，此前任何"tick 触发动画"的方案都会一跳一跳；现改为记录 tick 锚点后由 TimelineView 按帧插值（30fps），显示进度与 tick 到达时刻完全无关，天然连续；拖动小球实时跟手。
- 修复点击歌词/进度条跳转后进度显示与时钟"乒乓"冲突的问题：seek 落位完成前丢弃观察器的旧位置回调，跳转后进度条不再回跳抖动。

- 窗口左上角的红黄绿三个系统按钮整体往右、往下微调，不再紧贴窗口左上角。
- 首页快捷操作顺序调整为："继续播放"在最前，其后依次是"播放全部""随机播放"，三个按钮使用同一套悬停动效。
- "继续播放"跨启动记忆说明：设置 → 播放记忆（默认开启）会保存关闭软件时的歌曲、队列与进度；重新打开后点"继续播放"即从上次位置接着播。

- 歌曲列表排序改为点击列头完成：点击"标题"按歌名字母序、"专辑"按专辑名字母序、"时长"按时长排列；再点同一列头切换升/降序。激活列头右侧显示深色小尖角（升序向上、降序向下），列头文字位置和颜色不受影响。
- 歌曲页搜索框左侧的原排序按钮已移除，其功能融合进"标题 / 专辑 / 时长"三个列头；未点击列头时保持原有的添加时间顺序。

- 界面风格向椒盐音乐式的扁平简洁靠拢：侧栏去掉渐变玻璃底改为纯色平面，选中项改为主题色浅色圆角块（移除左侧竖条）；底部播放条改为实色平面。首页"推荐曲目"卡片保留原有玻璃质感。
- 新增统一设计令牌：`HPMetrics` 圆角层级（行 10 / 卡片 16 / 瓦片 20）与 `Color.hpHairline` 发丝线，后续视图统一引用。
- 全应用交互动效升级（对齐 Apple Music 风格）：默认无背景的按钮悬停时以圆角矩形渐显主题色高亮，按下加深；主操作按钮悬停轻微提亮上浮。侧栏导航、设置页签、详情页工具按钮、显示切换器、歌词样式按钮均已接入。
- 所有交互按钮取消椭圆（胶囊）造型，统一改为圆角矩形；主操作按钮为实色圆角矩形。
- "最近添加"列表模式去除歌曲行下的常驻灰底，改为悬停时才显示高亮，与底色更融合。
- "最近添加"改为按歌曲展示：卡片显示歌曲名与演唱者（不再显示专辑名），封面调小、歌名与演唱者更紧凑。
- "最近添加"右上角新增显示方式切换：封面网格 / 横向文件名列表，选择会记住。
- 首页改版：移除"最近播放"横滑区块，"最近添加"改为纵向平铺网格（每行约 5~6 张卡片，随窗口宽度自适应，不再横向滚动）。
- 首页快捷操作新增"继续播放"按钮（恢复上次播放进度）。
- 切换白天/夜间模式时，左上角品牌图标同步切换：白天显示白色图标，夜间显示深色图标，并带淡入过渡（遵循"减弱动态效果"设置）。
- 左上角品牌图标不再受设置中"应用图标"风格固定的限制；该设置现在只影响 Dock 图标。
- 修复"每次打开软件时更新"的首页推荐在切换页面后被重新随机的问题：推荐现在在整个运行期间保持稳定，重启应用后才更换。
- 设置中的推荐频率选项更名为"每次打开软件时更新 / 每天更新"，并说明切换页面时推荐保持不变。
- 大曲库浏览更流畅：列表、专辑、歌手、文件夹的分组与搜索改为后台预计算，一次事务刷新界面。
- 封面按显示尺寸三级降采样加载，长列表滚动更快、内存更低；小尺寸缩略图不再叠加投影。
- 播放进度与睡眠定时器倒计时独立刷新，不再触发整个界面重算。
- 播放/暂停图标与详情页切换动画遵循系统"减弱动态效果"设置。
- 新增 20 个单元测试，覆盖资料库持久化、浏览快照、封面管线与播放时钟隔离。

## 3.10.0 - 2026-09-13

- 播放页窗口标题栏改为完全透明，顶部不再出现独立色带；返回按钮改为左上角圆角 V 形，并补充随机、循环控制。
- 播放页布局按窗口尺寸伸缩，减少底部留白；歌词样式面板增加行距和显示行数。
- 调整开发流程为一功能一分支，合并和发布完成后删除已用完的分支。
- 增加功能完成或成功合并后的固定结束语规则。

## 3.9.0 - 2026-09-13

- 新增按版本独立保存的发布记录、历史版本归档和发布模板。
- 修复 README 被源码内容覆盖的问题，恢复项目说明并补充文档索引。
- 放大播放主界面左侧封面与下方控制条，并优化歌词渐隐和当前行强调效果。
- 调整 Dock 专辑封面留白，缩小封面并扩大亚克力背板的可视边框。
- 设置中新增“记住上次播放状态”，重启后恢复歌曲、队列、进度和音量。
- 启动焦点默认落在播放按钮，未编辑搜索框时按空格可播放或暂停。
- 修复歌曲列表顶部大块空白，列表首行紧跟表头。
- 首页“最近播放”在当前停留期间保持顺序，不再随切歌跳动。
- Dock 专辑封面增加窄幅玻璃背板，兼容不同尺寸封面并遮挡白边。
- 顶部“漫域音乐”品牌和图标支持点击返回首页。
- 切歌时保留上一张封面，消除 Dock 图标闪回默认图标。
- 设置界面改为与主界面一致的白蓝玻璃卡片。
- 设置界面改为主窗口内嵌页面，不再打开独立设置窗口。
- 应用菜单和快捷键设置入口改为切换到主窗口的设置页面。
- FLAC 等格式标签移动到歌曲信息右侧。
- Dock 专辑封面角标改为封面取色的玻璃亚克力样式，并放大播放/暂停符号。
- FLAC 等格式标签移动到歌曲名和歌手右侧。
- 设置入口移动到左侧栏底部。
- 主界面移除“导入”和“添加音乐”按钮，音乐导入统一放入设置。
- 底部播放栏缩为 72pt，并缩小专辑封面和播放按钮。
- 首页歌词摘要固定展示 4 行。
- 首页播放按钮移入专辑封面内部，并根据封面主色自动配色。
- 首页副标题只显示歌曲总数，不再显示专辑数量。
- 首页歌词竖线与文本严格等高，避免底部空白。
- 首页专辑封面增加玻璃材质衬托层。
- 播放状态按钮改为无白色描边的玻璃亚克力样式。
- 首页歌词摘要改为优先截取歌词中段，并过滤制作人员信息。
- 首页专辑封面支持点击进入完整播放界面，右下角状态按钮支持播放/暂停。
- 首页问候语增加中文诗句、简短问候和英文短句轮换。
- 增加深色、浅色两套 App 图标资源。
- 设置中增加“自动 / 深色 / 浅色”三档 App 图标选择。
- Dock 基础图标和应用侧栏图标会跟随图标设置。
- Dock 专辑封面保留为播放设置中的独立开关，不放入图标选择。
- 修复侧栏品牌图标与 App 图标不一致。
- 增加强制分支开发、合并提醒和功能文档规则。

本项目使用语义化版本号：`主版本.次版本.修订版本`。

## 3.8.0 - 2026-09-13

- 修复 macOS Dock 图标白边和底部裁切问题
- 重新生成透明圆角、多尺寸 AppIcon
- 新增设置窗口，可从侧边栏齿轮或系统设置菜单进入
- 新增播放时在 Dock 显示专辑封面
- Dock 专辑封面增加白色播放/暂停状态角标
- 首页推荐歌词改为连续显示 3～4 行
- 歌曲列表和首页封面右下角统一使用自适应播放状态图标
- 修复歌词页控件底色突兀、蓝色光晕和标题栏冗余问题
- 歌词支持字号、字体、颜色自定义
- 播放页背景和控件跟随专辑封面主色
- 优化主界面顶部留白和窗口尺寸兼容

## 3.7.0

- 新增设置界面
- 增加 Dock 专辑封面开关

## 3.6.0

- 更换为深蓝紫播放图标
- 侧栏品牌标识同步更新

## 3.5.0

- 首页改为歌词摘要加专辑封面
- 歌词样式可调整
- 主界面到播放页增加封面过渡动画

## 3.0.0

- 新增首页、用户歌单、播放历史
- 新增艺术家详情、排序、播放队列和睡眠定时
